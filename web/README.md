# EduReels Web

Next.js App Router + shadcn/ui enterprise frontend.

```bash
cp .env.example .env.local
npm install
npm run dev   # http://localhost:3001
```

See root [README](../README.md) for Docker Compose smoke steps.
