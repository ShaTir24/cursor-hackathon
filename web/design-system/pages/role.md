# Role (`/role`)

**Job:** Choose Student or Teacher with equal visual weight.

**Layout:** Page title + two radio cards (not faded stagger victims). Selected state = primary ring/border. Keyboard focus visible.

**Testids:** `role-student`, `role-teacher`. Navigate to `/onboarding?role=…`.
