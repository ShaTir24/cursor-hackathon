# App shell

**Job:** Workspace wayfinding + Learn/Teach persona chrome.

**Layout:** Sidebar logo lockup; primary nav (Home or Role/Setup); Account group when onboarded; footer with avatar + name + Sign out. `SidebarInset` uses subtle mesh wash. Header eyebrow = Learn or Teach when complete.

**No new routes.** Sign out → `/login`.
