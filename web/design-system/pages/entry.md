# Entry (`/`)

**Job:** Get authenticated users into Sign in / Create account.

**Layout:** Full-bleed `edu-mesh`; hero-level EduReelsLogo; one headline; one supporting sentence; two CTAs only. No stats, card grids, or secondary marketing.

**CTAs:** Sign in → `/login`; Create account → `/login?mode=signup` (`entry-signin`, `entry-signup`).

**Auth freeze:** Do not change login/signup pages or auth-form.
