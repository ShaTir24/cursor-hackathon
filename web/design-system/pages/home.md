# Home (`/home`)

**Job:** Generate a reel from the user’s focus/classroom.

**Layout:** Eyebrow (Learn/Teach) → greeting → focus badges → stage panel with primary Generate (`generate-submit`) → recent list via Item; Empty when none.

**API:** Student `createVideo({})`; Teacher `createVideo({ includeLessonPack: true })`.
