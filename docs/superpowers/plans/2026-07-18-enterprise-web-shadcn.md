# Enterprise Web shadcn Implementation Plan

> **For agentic workers:** Use subagent-driven-development or executing-plans.

**Goal:** Next.js + shadcn enterprise UI wired to Nest/Supabase + Docker Compose.

**Architecture:** `web/` App Router; Bearer JWT; compose `api`+`web`.

**Tech:** Next.js 15, Tailwind, shadcn/ui, hls.js, Supabase JS, Docker.

---

Phases 0–6 tracked in repo todos; implement under `web/` + root compose.
