{
  "name": "edureels",
  "private": true,
  "scripts": {
    "web": "npm run dev --prefix web",
    "backend": "npm run start:dev --prefix backend",
    "dev": "npx concurrently -k \"npm run backend\" \"npm run web\"",
    "test:backend": "npm test --prefix backend",
    "sanity:onboarding": "npx ts-node --transpile-only backend/scripts/sanity-onboarding.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
